# simple_Blockchain_JS
A simple example of a block chain written in javascript

## Run program
Make sure to have node installed and download the repo and at the root dir install packages using
```
npm i
```
then run the program with
```
node main.js
```
